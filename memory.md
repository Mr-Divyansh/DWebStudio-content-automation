# D Web Studio Content Automation — Project Memory

## 1. Stable Project Facts

**Project repository:** `Mr-Divyansh/client-content-automation` (local folder: `DWebStudio-content-automation`)

**Primary content domain:** Instagram content automation.

**Current purpose:** Collect client marketing information and support generation of Instagram posts, Reels, captions, hooks, CTAs, and hashtags.

---

## 2. Current Status

**Documentation foundation:** Created.

**Implementation:** Not started.

**Git workflow:** Local repository initialized (`main` branch) with `origin` pointing to `https://github.com/Mr-Divyansh/client-content-automation`. The owner runs `git add` / `git commit` / `git push` manually after reviewing changes; nothing is staged, committed, or pushed automatically.

---

## 3. Current Decisions

- Documentation follows the layered approach used by the D Web Studio project.
- Governing documentation is kept at repository root.
- Reusable generation prompts live in `prompts/`.
- Unresolved decisions must be recorded instead of guessed.

---

## 4. Open Decisions

- application type;
- technology stack;
- content-generation provider/API;
- client data storage;
- authentication and multi-client handling;
- final intake fields;
- output/export format;
- human approval workflow;
- Instagram publishing/automation method;
- product name and visual identity.

---

## 5. Long-Term Direction

The project should evolve without losing the separation between requirements, architecture, design, rules, memory, phases, and AI operating procedure.
